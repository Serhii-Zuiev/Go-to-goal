import React from 'react'
import styles from './banner.module.css'

const Banner = () => {
    return (
        <section className={styles.bannerSection}>
            <p hidden>Our Banner</p>
        </section>
    );
}

export default Banner;